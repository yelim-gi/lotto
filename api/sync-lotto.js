import {db,verify,cleanDraw} from './_lib.js';export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'POST only'});try{if(!verify(req.body?.secret))return res.status(401).json({error:'관리 비밀번호가 올바르지 않습니다.'});const r=await fetch(process.env.LOTTO_DATA_URL||'https://smok95.github.io/lotto/results/all.json');if(!r.ok)throw new Error(`외부 데이터 요청 실패: ${r.status}`);const rows=(await r.json()).map(x=>cleanDraw(x,'github'));const last=await db('lotto_draws?select=draw_no&order=draw_no.desc&limit=1');const newer=rows.filter(x=>x.draw_no>(last?.[0]?.draw_no||0));for(let i=0;i<newer.length;i+=200)await db('lotto_draws?on_conflict=draw_no',{method:'POST',headers:{Prefer:'resolution=merge-duplicates,return=minimal'},body:JSON.stringify(newer.slice(i,i+200))});return res.status(200).json({ok:true,inserted:newer.length,latest:rows.at(-1)?.draw_no});}catch(e){return res.status(400).json({error:e.message});}}