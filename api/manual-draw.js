import {db,verify,cleanDraw} from './_lib.js';export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'POST only'});try{if(!verify(req.body?.secret))return res.status(401).json({error:'관리 비밀번호가 올바르지 않습니다.'});const row=cleanDraw(req.body,'manual');await db('lotto_draws?on_conflict=draw_no',{method:'POST',headers:{Prefer:'resolution=merge-duplicates,return=representation'},body:JSON.stringify(row)});return res.status(200).json({ok:true,draw:row});}catch(e){return res.status(400).json({error:e.message});}}